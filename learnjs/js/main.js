let title = `JS El Zero School`,
  dec = `Best School Ever`,
  data = `25/10`,
  div = `<div class="card">
  <h3> ${title}</h3>
  <p>${dec}</p>
  <span>${data}</span>
  </div>
  `;
document.write(div.repeat(4));
console.log(`${title}
${data}`)